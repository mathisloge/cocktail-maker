{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Recipe",
    "type": "object",
    "required": [
        "name",
        "steps"
    ],
    "properties": {
        "$schema": {
            "type": "string"
        },
        "name": {
            "type": "string",
            "description": "The name of the recipe"
        },
        "tags": {
            "type": "array",
            "description": "A list of tags categorizing the recipe",
            "items": {
                "type": "string"
            }
        },
        "description": {
            "type": "string",
            "description": "A short description of the recipe"
        },
        "nominal_serving_volume": {
            "type": "number",
            "description": "The expected total serving volume in milliliters"
        },
        "steps": {
            "type": "array",
            "description": "An ordered list of steps — dispense, manual, or a group of parallel dispense steps",
            "items": {
                "oneOf": [
                    {
                        "$ref": "#/$defs/dispenseStep"
                    },
                    {
                        "$ref": "#/$defs/parallelSteps"
                    },
                    {
                        "$ref": "#/$defs/manualStep"
                    }
                ]
            }
        }
    },
    "$defs": {
        "dispenseStep": {
            "title": "DispenseStep",
            "type": "object",
            "description": "A single step that dispenses one ingredient",
            "required": [
                "kind",
                "ingredient",
                "volume"
            ],
            "additionalProperties": false,
            "properties": {
                "kind": {
                    "type": "string",
                    "const": "dispense"
                },
                "ingredient": {
                    "type": "string",
                    "description": "The identifier of the ingredient to dispense"
                },
                "volume": {
                    "type": "number",
                    "description": "The volume to dispense in milliliters",
                    "minimum": 1
                }
            }
        },
        "parallelSteps": {
            "title": "ParallelSteps",
            "type": "object",
            "description": "A group of dispense steps to be executed in parallel (manual steps are not allowed here)",
            "required": [
                "parallel-steps"
            ],
            "additionalProperties": false,
            "properties": {
                "parallel-steps": {
                    "type": "array",
                    "description": "Two or more dispense steps to run simultaneously",
                    "minItems": 2,
                    "items": {
                        "$ref": "#/$defs/dispenseStep"
                    }
                }
            }
        },
        "manualStep": {
            "title": "ManualStep",
            "type": "object",
            "description": "A manual instruction for the user (cannot appear inside parallel-steps)",
            "required": [
                "kind",
                "text"
            ],
            "additionalProperties": false,
            "properties": {
                "kind": {
                    "type": "string",
                    "const": "manual"
                },
                "text": {
                    "type": "string",
                    "description": "The instruction text shown to the user"
                }
            }
        }
    }
}