import { Palette } from "../palette.slint";

export component LargeText inherits Text {
    in property <bool> primary;
    color: primary ? Palette.text-primary : Palette.text-muted;
    letter-spacing: 1.5pt;
    font-size: 32pt;
    horizontal-alignment: center;
}
