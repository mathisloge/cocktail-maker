
export enum Page { cocktail_list_page, cocktail_detail_page, mix_page, finished_page  }

export struct RecipeView {
    name: string,
    tag_line: [string],
    description: string,
    image: image,
}
